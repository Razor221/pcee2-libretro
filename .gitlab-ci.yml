# DESCRIPTION: GitLab CI/CD for libRetro (NOT FOR GitLab-proper)
#
# This drives the libretro buildbot, the same way nuance-linux-libretro does.
#
# NOTE: unlike most libretro cores, pcee2 (a PCSX2 fork) is heavy:
#   * it needs a set of pre-built dependencies (shaderc, etc.) produced by
#     pcee2-libretro/scripts/build-deps-linux.sh into $CI_PROJECT_DIR/deps,
#   * it builds with clang + Ninja,
#   * it is currently Linux x86_64 + Vulkan only and flagged experimental
#     (see pcee2-libretro/pcee2_libretro.info).
# The build flags below mirror .github/workflows/libretro_builds.yml.

##############################################################################
################################# BOILERPLATE ################################
##############################################################################

.core-defs:
  variables:
    GIT_SUBMODULE_STRATEGY: recursive
    CORENAME: pcee2
    # cmake configure args (passed by the libretro cmake template as CORE_ARGS)
    # NOTE: PCSX2 officially supports only Clang/MSVC, but the libretro build
    # images ship ancient apt clang (3.8 on xenial / 6.0 on bionic) with no C++20
    # and too old a glibc to drop in a modern clang. The images' native gcc9 is
    # the only C++20-capable compiler here, so build with it (no clang override).
    CORE_ARGS: >-
      -DENABLE_LIBRETRO=ON
      -DENABLE_QT_UI=OFF
      -DENABLE_TESTS=OFF
      -DDISABLE_ADVANCE_SIMD=ON
      -DSHADERC_STATIC=ON
      -DCMAKE_PREFIX_PATH=$CI_PROJECT_DIR/deps
      -DSHADERC_LIBRARY=$CI_PROJECT_DIR/deps/lib/libshaderc_combined.a
      -DCMAKE_FIND_ROOT_PATH_MODE_LIBRARY=BOTH
      -DCMAKE_FIND_ROOT_PATH_MODE_INCLUDE=BOTH
      -DCMAKE_FIND_ROOT_PATH_MODE_PACKAGE=BOTH
    # the libretro target name (output: build/bin/pcee2_libretro.so)
    MAKE_ARGS: pcee2-libretro

include:
  - project: 'libretro-infrastructure/ci-templates'
    file: '/linux-cmake.yml'
  # Windows MinGW cross-compile template; provides .libretro-windows-cmake-x86_64
  # used by libretro-build-windows-x64 below. Without this include the whole
  # pipeline fails at config time (undefined `extends` target).
  - project: 'libretro-infrastructure/ci-templates'
    file: '/windows-cmake-mingw.yml'

# The stock libretro build images are ancient Ubuntu (xenial/bionic): their
# CMake (3.20), gcc, and system libs (PNG 1.2, zstd, webp, lz4) are far too old
# for PCSX2's pinned versions (PNG 1.6.40, zstd 1.5.5, ...). Override the image
# with ubuntu:24.04, which satisfies all of them, and apt-install the toolchain
# plus the -dev packages the core's SearchForStuff.cmake looks for. SDL3,
# plutovg/plutosvg, ryml, shaderc and FreeType are still built from source by
# build-deps-linux.sh into $CI_PROJECT_DIR/deps.
.pcee2-setup-linux: &pcee2-setup-linux
  - export DEBIAN_FRONTEND=noninteractive
  - apt-get update
  # Package list mirrors .github/workflows/libretro_builds.yml (the authoritative
  # set), minus clang and libshaderc-dev (we build with gcc and build shaderc /
  # FreeType from source), plus the basics needed to run the deps build.
  - apt-get install -y --no-install-recommends
      git ca-certificates curl build-essential cmake ninja-build python3 pkg-config extra-cmake-modules
      liblz4-dev libwebp-dev libcurl4-openssl-dev libpcap-dev libfontconfig-dev
      libudev-dev libx11-dev libxrandr-dev libxext-dev libwayland-dev libegl-dev libdbus-1-dev
      libjpeg-turbo8-dev libpng-dev zlib1g-dev libzstd-dev libdrm-dev libxkbcommon-dev
      libpipewire-0.3-dev libpulse-dev libasound2-dev
      libxcursor-dev libxi-dev libxinerama-dev libxfixes-dev libxrender-dev
      libxss-dev libxtst-dev libgl1-mesa-dev libdecor-0-dev libgbm-dev
  - cmake --version && gcc --version
  # The libretro template builds with `-j $NUMPROC`; our before_script overrides
  # the one that sets it, so cap it ourselves by available RAM (~2 GB per gcc13
  # job) to avoid OOM-killing cc1plus on the big PCSX2 translation units.
  - NUMPROC=$(nproc); MEMGB=$(awk '/MemTotal/{print int($2/1024/1024)}' /proc/meminfo)
  - CAP=$((MEMGB/2)); [ "$CAP" -lt 1 ] && CAP=1; [ "$NUMPROC" -gt "$CAP" ] && NUMPROC=$CAP
  - export NUMPROC; echo "NUMPROC=$NUMPROC (cores=$(nproc), mem=${MEMGB}GB)"

# musl (Alpine) aarch64 setup. The glibc aarch64 build above won't load on
# musl distros (Alpine, postmarketOS), so this builds the same core in an Alpine
# image with apk. The package set is the apt list above mapped to apk (mesa-dev
# covers the EGL/GL/gbm headers). -static-libstdc++/-static-libgcc keeps the GCC
# runtime out of the core's NEEDED list. SDL3/FreeType/shaderc/etc. are still
# built from source by build-deps-linux.sh.
.pcee2-setup-musl: &pcee2-setup-musl
  # The runner's shell PATH lacks /sbin, where Alpine's apk lives (apt-get on the
  # glibc jobs is in /usr/bin, so it was unaffected); add it so apk is found.
  - export PATH="/sbin:/usr/sbin:$PATH"
  - apk update
  - apk add --no-cache
      bash git ca-certificates curl build-base cmake samurai python3 pkgconf extra-cmake-modules linux-headers
      lz4-dev libwebp-dev curl-dev libpcap-dev fontconfig-dev eudev-dev
      libx11-dev libxrandr-dev libxext-dev wayland-dev mesa-dev dbus-dev
      libjpeg-turbo-dev libpng-dev zlib-dev zstd-dev libdrm-dev libxkbcommon-dev
      pipewire-dev pulseaudio-dev alsa-lib-dev
      libxcursor-dev libxi-dev libxinerama-dev libxfixes-dev libxrender-dev
      libxscrnsaver-dev libxtst-dev libdecor-dev
  - cmake --version && gcc --version
  - export LDFLAGS="-static-libstdc++ -static-libgcc"
  - NUMPROC=$(nproc); MEMGB=$(awk '/MemTotal/{print int($2/1024/1024)}' /proc/meminfo)
  - CAP=$((MEMGB/2)); [ "$CAP" -lt 1 ] && CAP=1; [ "$NUMPROC" -gt "$CAP" ] && NUMPROC=$CAP
  - export NUMPROC; echo "NUMPROC=$NUMPROC (cores=$(nproc), mem=${MEMGB}GB)"

# Build the dependency tree (shaderc, ...) before configuring the core.
# Runs in the libretro build image; mirrors the GitHub Actions deps step.
.pcee2-deps-linux: &pcee2-deps-linux
  - bash pcee2-libretro/scripts/build-deps-linux.sh "$CI_PROJECT_DIR/deps"

# Windows deps: same script, but cross-compiled for MinGW via the MXE cmake
# wrapper the libretro Windows image provides. CMAKE drives the CMake deps and
# HOST the autoconf target triple (libbacktrace).
.pcee2-deps-win: &pcee2-deps-win
  - export CMAKE=x86_64-w64-mingw32.static-cmake
  - export HOST=x86_64-w64-mingw32
  # The windows-cmake template's before_script (which sets NUMPROC=nproc/5) is
  # replaced by this one, so set NUMPROC ourselves; cap by RAM as on Linux to
  # avoid OOM-killing the cross-gcc on the big PCSX2 translation units.
  - NUMPROC=$(nproc); MEMGB=$(awk '/MemTotal/{print int($2/1024/1024)}' /proc/meminfo)
  - CAP=$((MEMGB/2)); [ "$CAP" -lt 1 ] && CAP=1; [ "$NUMPROC" -gt "$CAP" ] && NUMPROC=$CAP
  - export NUMPROC; echo "NUMPROC=$NUMPROC (cores=$(nproc), mem=${MEMGB}GB)"
  - bash pcee2-libretro/scripts/build-deps-linux.sh "$CI_PROJECT_DIR/deps"

stages:
  - build-prepare
  - build-shared

##############################################################################
#################################### STAGES ##################################
##############################################################################

# Linux 64-bit (x86_64).
libretro-build-linux-x64:
  image: ubuntu:24.04
  extends:
    - .libretro-linux-cmake-x86_64
    - .core-defs
  before_script:
    - *pcee2-setup-linux
    - *pcee2-deps-linux

# Linux ARM 64-bit (aarch64). pcee2 ships aarch64 builds; the buildbot
# provides an aarch64 image/runner via .libretro-linux-cmake-aarch64.
libretro-build-linux-aarch64:
  image: ubuntu:24.04
  extends:
    - .libretro-linux-cmake-aarch64
    - .core-defs
  before_script:
    - *pcee2-setup-linux
    - *pcee2-deps-linux

# Linux ARM 64-bit (aarch64), musl/Alpine. Produces a core that loads on musl
# distros (Alpine, postmarketOS) where the glibc aarch64 build cannot.
libretro-build-linux-aarch64-musl:
  image: alpine:3.21
  extends:
    - .libretro-linux-cmake-aarch64
    - .core-defs
  before_script:
    - *pcee2-setup-musl
    - *pcee2-deps-linux

# Windows 64-bit (MinGW cross-compile via the libretro MXE image). NOTE: pcee2
# is a PCSX2 fork and PCSX2 officially targets MSVC/clang-cl, so this MinGW
# attempt may not build PCSX2; tracked here to see how far it gets.
libretro-build-windows-x64:
  # Experimental MinGW cross-build: don't fail the pipeline (x64/aarch64 are the
  # shipping targets). Still built and visible so we can track how far it gets.
  allow_failure: true
  extends:
    - .libretro-windows-cmake-x86_64
    - .core-defs
  variables:
    # MXE's MinGW toolchain ships libpng 1.6.37; SearchForStuff.cmake's 1.6.40
    # pin is conservative (PCSX2 only uses libpng for screenshots/textures), so
    # relax the minimum for this cross-build only. x64/aarch64/Mac keep 1.6.40.
    PCSX2_PNG_MIN_VERSION: "1.6.37"
  before_script:
    - *pcee2-deps-win
